# Codex执行任务：Swarmalator–CAV Stage1B
## 共同基础修正、有限窗口可行性诊断与配对重跑

你继续在当前Swarmalator–CAV工作区执行研究。本轮不是再次制定计划，也不是开始SUMO/CARLA/MARL。请完成代码修改、真实测试、可行性诊断、配对实验和可复现反馈包。

研究主线不变：检验间隙形成与内部机动进程的双向耦合、以及角色间非互易分配，是否能在有限窗口内产生有净交通价值的协作。不要预设D获胜，也不要因为先导结果未占优就直接更换研究主题。

## 1. 当前证据与读取顺序

先核对实际工作区、AGENTS.md、Stage1代码/config/报告，不猜测路径，不覆盖已有结果。若当前目录与用户所述`F:/Swarmalator–CAV`不同，以实际打开的目录为准并简要说明。

读取：
- `src/swarmalator_cav/simulation.py`
- `src/swarmalator_cav/run_stage1.py`
- `configs/stage1_config.json`
- `docs/model_v0.md`
- `tests/test_simulation.py`
- `reports/stage1_report.md`
- `outputs/stage1/metrics.csv`、`events.csv`及所需轨迹
- 用户放入的`stage1_review_and_next_step.md`与相应审阅包（若存在）。

参考PDF若已放入references则阅读必要模型/控制部分；若没有，不声称已经阅读全文，也不因重复文献搜索阻塞本轮。

Stage1为18个确定性环境×5方法=90次。已报告A完成8/18，B/C/D/E各6/18。独立审阅中4项测试通过，90次结果得到复现；除运行耗时外，指标与交付CSV在浮点精度内一致。以下是对“所上传代码快照”的发现，请对你当前版本核实，不将行号当作版本不变的保证。

## 2. 必须先处理的共同基础问题

### 2.1 移动参考的速度表达

当前A–D使用：

```text
v_ref_M = clip((s_ref - s_M)/2.4, 17, 23)
a_nom_M = 0.25*(s_ref - s_M) + 0.60*(v_ref_M-v_M)
```

在原72次A–D运行的整个PREPARE/EXECUTE阶段，v_ref_M始终为17。位置误差为零、M及参考均以18m/s移动时，该式仍给出-0.6m/s²，暴露相对位置纠偏与绝对移动速度混用的问题。

请建立自洽的移动参考跟踪：显式区分参考位置、参考速度、参考加速度与误差纠正。可采用参考运动前馈＋误差反馈，或等价结构，但必须解释量纲和零误差平衡。

对BCD，若`s_ref=c+(1-q)e_M0`，需考虑参考导数中c和q的变化；避免在qdot与a_nom相互依赖时引入未定义代数循环。使用同快照可计算量或清楚的离散更新，不读取未来轨迹。

所有方法共用等价的低层跟踪接口，不得只给D加入前馈。E的滚动候选时刻、平滑时刻和相应位置/速度目标也应保持一致。不要通过提高强制速度下限、给D额外状态或单独放松动作限制来改善结果。

必须新增单元测试：参考匀速、位置/速度误差为零、约束不活跃时，不应出现无理由减速。

### 2.2 统一目标间隙的物理含义

当前A/E使用`G0+0.55ΔG`，BCD在q=1使用`G0+ΔG`；方法并非仅协调形式不同。short_window的BCD终值仅20.2m或17.2m；按原车长与动态余量，在M/R均18m/s时需要21.4m才能得到非空插入区间。原30次short_window的整个轨迹中该区间均为负。

请从同一车身、安全余量、预估速度及任务目标推导所有方法共享的最终空间要求。准备过程可以不同，但不得让某方法被分配更小/更大任务目标而不披露。

目标与约束分开：启发式ΔG、相位门槛、参考速度偏好不是物理可行性的限制。不要通过缩小安全余量、移动车辆初始状态或任意延长道路来让方法获胜。确需调整模型定义时，另列新版本和旧/新影响。

### 2.3 启动条件必须检查整个剩余机动，而不是仅当前间隙

原60次启动中28次全部因为未完成横移就超过173m而失败。对28个失败启动快照，使用当前离散加速度/jerk边界计算的3秒最少前進距离，已有24个超过剩余距离。这只是说明“这些实际启动状态过晚”，并不说明初始场景无解。

当前`merge_zone=(55,170)`、事件终点170+3、`terminal_s=190`和观察道路450之间含义不一致。请明确：可开始区、必须完成位置、失败处置空间、观测范围。统一一处定义，解释原3m容差和190m字段的用途，不能仅删除失败判据来提高完成率。

所有方法使用相同的物理启动/执行可行性检查，至少排除“按允许最大制动也不可能在剩余位置完成固定3秒横移”的状态。进一步检查占用期间的前后间距和执行约束，不把通过必要条件当作充分安全保证。

相位门槛可保留为某算法的决策规则，但必须单独标注，不能计入物理可行性定义。原审阅中去掉BCD的q>=0.45门槛后，三者仍各6/18，因此不要预设只去掉门槛就能解决失败。

### 2.4 修正任务终止后的运动

原代码仅在EXECUTE更新横向进度，所以全部28次部分横移失败后都停在中间横向位置、继续纵向驶到16秒；成功/失败后仍输出旧任务的空间目标，只冻结q。

请明确准备、执行、成功释放、失败处置的状态变化。成功后采用统一正常跟驰/巡航；失败或不可完成时采用明确、连续、符合当前模型的安全处置。不能让车辆永久冻结在跨车道中间；不能位置瞬移；不能在不允许的条件下强制完成横移。

车辆在机动开始后的物理横向进度与内部q保持独立。若选择回撤/继续/停车的某一种处置，解释适用条件、可用道路和执行代价，并对所有方法一致。

固定观测窗保留失败车辆，不删除失败后代价。同时拆分准备、执行、成功后与失败处置阶段成本，以免把任务收益和终止后行为混在一起。

### 2.5 安全执行与统计口径

目前安全修正先算，jerk限制后算。请检查后续jerk/速度裁剪是否破坏前一层预测，并以实际执行轨迹重新验算。可以使用轻量的jerk可行控制区间、短时域检查等；本轮不要求建立全局CBF定理或生产级安全架构。无解/回退要如实记录。

`∫max(0,v_des-v)dt`单位是m，不是m·s；在新版本修正名称与图注，保留旧字段映射，不改旧表来掩盖错误。它不是旅行时间延误。

距离使用位置差或与积分器一致的积分；角色最小净距要实际分别计算，不再向M/F/B回填全局最小值。纵向jerk不能写成完整二维舒适性。

日志末行记录真实控制状态，不再统一插入虚假的0增益和`G0+0.55ΔG`目标。保留名义/安全目标/实际动作，安全修正与一般动作饱和分别统计。

## 3. 可行性诊断：保持小而有用

不要立即做全维Hamilton–Jacobi可达集或建立复杂求解证书系统。本轮只处理原18个环境及少量必要的代表性快照，目的是分清物理限制、过晚决策和控制器未找到轨迹。

### 3.1 三种结论必须区分

- `feasible_witness`：找到满足明确模型约束的完整轨迹，并经独立重放/检查验证；
- `excluded_by_necessary_bound`：有效的乐观外包络或必要条件已将当前问题排除，注明排除范围/假设；
- `unresolved`：没有见证，也没有有效排除依据。

不得将局部优化失败、候选库未命中、超过计算预算、或者某个参考控制器失败写成“物理不可行”。多个独立位置区间分别相交也不是联合可行轨迹。

### 3.2 约束与信息范围

固定同一目标间隙、车身、道路、固定横移时长/路径族、速度、加速度、jerk和观察窗口，F使用原环境响应，B闭环响应R。独立见证搜索不受q门槛或手设目标ΔG限制，也不能忽略B和机动之后的安全。

以离散动力学为主时，结论注明离散模型，不冒充连续时间或真实道路保证。独立检查器至少重新核对动力学残差、动作边界、几何占用及终点，必要时用更细步长检查边界状态。

若离线搜索知道F未来扰动脚本，注明其全知诊断身份；这些未来信息不能进入在线A–E，不将离线参考直接当公平在线基线。

先利用已有成功轨迹作见证候选，再对未定环境采用有界分段控制搜索或小规模轨迹优化。搜索方法由你根据环境选择，以尽快得到可检查证据为目标，不无限增加参数和候选。

## 4. 协调机制诊断与有限修订

原审阅中，C与D的M/R q最大差约0.0081，位置最大差约0.0298m，最终成本均值只差0.0020m。约一半记录的空间反馈绝对量来自q已饱和或任务已终止时，因此非零原始项不等于有效控制作用。

请分别记录：
1. 原始空间反馈/伙伴项；
2. 限幅后真实Δq；
3. q或K扰动对名义空间目标、名义动作、实际动作的影响；
4. PREPARE、EXECUTE、终止后及q饱和区间。

做同快照干预，区分：本车空间梯度反馈、W(X)这个空间依赖、伙伴相位作用、非互易K分配。不把一次同时删除多个通道的比较当作单机制消融。

D原能力分数用`a_max-abs(a_nom)`等代理，没有区分加减速余量，也没使用B状态。请检查其是否对应当前需要的调整方向。必要时用透明的方向性动作/时机余量替代，记录信息来源和计算成本；保持成对总增益可比，避免单纯增加强度。若加入B信息，应给予所有相关基线同等观测权限。

先完成共同基础修正和原模型诊断。若有限q早饱和使“持续空间—进程协调”在关键阶段事实上停止，可在报告中明确其只是准备进程；或者提出至多一个有明确数学理由的进程修订候选，作为单独版本比较。不得为制造非零相位差而人为振荡、注入无关扰动或按测试结果反复更换理论。

原审阅还发现：C去掉伙伴项仍6/18，成本均值约变化-0.0354m；C去掉梯度反馈仍6/18，成本约变化+0.2951m（后者仍有W(X)空间路径）。这些结果只用于定位，不能当作正式优越性结论。

## 5. 实验顺序与公平比较

按顺序完成：

A. 保存Stage1原样；先复现并建立本轮变更说明。
B. 添加针对移动参考平衡、过晚启动、失败后横向行为、目标间隙和有效反馈统计的必要测试。
C. 在原18个环境上，用修正后的共同基础运行A–E，形成新的90次配对结果。旧版本和新版本分开，不能将不同环境定义的数值混在一张排名表。
D. 完成小规模可行性诊断及根因分类。
E. 按结果做少量关键通道干预；确需局部调参时预先给所有相关方法同等开发预算，保留候选记录，并留少量未用于选择的初始状态作检查。不开展无限调参或新增大规模随机矩阵。

比较成功总数时也比较成功集合。原E和D虽然都是6/18，但仅4个环境共同成功，各有2个独有成功；E成本在18个配对环境中均更低。不得据同样成功数量声称“完成了同样任务”，也不得因E没有更高总体完成率而忽略成本优势。

共同修改带来的改进属于实验/执行基础改进，不算D的专属贡献。若修正后A或E表现更好，保留并分析其原因；若D有优势，应指出在何种已判定条件下、通过哪个机制出现。

## 6. 交付要求

建议使用：
- `docs/model_v1.md`：实际模型、与v0区别、信息路径、任务/道路边界；
- `reports/stage1b_report.md`：关键发现、真实结果、独立可行性分层、目前创新是否有证据；
- `outputs/stage1b/`：配置、90次结果、可行性与干预结果、任务事件和代表轨迹；
- `feedback/stage1b_feedback.zip`：代码、必要测试/config、模型说明、报告、轻量表格、关键轨迹、图及运行日志。

图最多三组，分别回答：
1. 任务剩余时间/距离与启动可行性；
2. 有效q反馈及其空间动作效应；
3. 可行性分层内各方法的成功与全部车辆代价。

全量轨迹留在本地，反馈包只包含必要子集；不打包虚拟环境、密钥、缓存、论文全文或无关文件。

最终返回：真实测试与运行数量、旧/新逻辑差异、哪些问题已经修复、哪些仍属科学假设、可行性三类计数、匹配比较结果、复现命令、反馈包路径，以及下一轮最值得做的一件事。

你可在当前权限内独立读取/写入本项目、安装必要轻量依赖、运行测试与修复，无需每步请求确认。保留原结果，不创建繁重审计框架。不在写完计划或未运行代码时结束；达到本轮交付范围后停止扩大实验。

现在开始执行。
