# Codex执行任务：Swarmalator–CAV Stage1C
## 已有可行任务上的闭环实现、公共约束一致性与核心模型辨识

请在当前Swarmalator–CAV工作区直接执行本轮研究，不要只输出计划。本文件自包含关键复核事实与执行要求；可选复核包没有放到本地也不构成阻断。不要再次把时间花在搜索缺失的`stage1_review_and_next_step.md`。不得假设可以访问此前ChatGPT对话。

## 一、研究目标与本轮边界

研究主题不变：以任务条件下的空间—内部机动进程耦合，联合组织M的到达时机与R的间隙调整，并检验非互易作用是否合理分配调整责任。M为汇入车，F为目标前车，R为协作后车，B为受影响的闭环跟驰车。

本轮不是通信鲁棒性实验，也不是第三轮大规模工程加固。核心问题是：

> 在现有离线候选已能完成的协作情境中，在线控制器为什么没有完成；进程状态是否真正控制了间隙形成与到达时机；进行一次针对性的模型修订后，能否在同等物理条件下形成可解释的闭环作用？

暂不加入通信延迟/噪声、SUMO、CARLA、MARL、高阶传播或新道路类型。无需全局可达集、连续时间安全总证明、复杂认证体系或大规模参数扫描。零结果也是本轮的有效研究产出，不预设D胜出。

先查看实际路径、AGENTS.md、现有改动和环境。保留Stage1、Stage1B源码、原outputs与反馈ZIP；新代码或清晰版本分支写入Stage1C，新结果写`outputs/stage1c/`。没有Git时保存一份简短变更记录即可，不建立繁重状态系统。允许在工作区和已有权限内安装轻量依赖、修复实现、运行测试和有限诊断，无需每步询问。重要覆盖、额外付费、真实权限阻断或根本改变研究问题时再确认。

## 二、输入与复核事实：请核查，不要机械继承

主要输入：
- `src/swarmalator_cav/stage1b_simulation.py`
- `src/swarmalator_cav/run_stage1b.py`
- `configs/stage1b_config.json`
- `docs/model_v1.md`
- `reports/stage1b_report.md`
- `outputs/stage1b/`内全部现有指标、事件与本地完整轨迹。

研究讨论窗口收到的Stage1B ZIP SHA256为
`36acff057d0d85959806a3a9b517c5f15f900edf4de9fc704be424c6615cd67d`。
其原始90主运行、36干预、18可行性行、56同快照行及3852关键轨迹行均已复现，排除计算耗时后与交付一致。8项测试通过需要旧模块simulation.py；该旧模块在完整工作区存在，但ZIP遗漏，后续轻包要包括必要运行依赖，或避免包级初始化不必要地导入旧模块。不要修改旧算法来迁就新版本。

以下是已经对实际代码/轨迹完成的复核，不是泛泛的风险清单：

### F1：最后一个横移步可能同时越过170m却仍被记成功

v1 `_task_event`先判断`s>completion_s AND merge_progress<1`，之后`merge_progress>=1`只检查间隙。四条reported completed实际上在170m之后完成：
- A_ample_prepare_i2：4.85s，170.666003m；
- B_ample_none_i2：4.70s，170.653345m；
- C_ample_none_i2：4.70s，170.560756m；
- D_ample_none_i2：4.70s，170.569805m。

这是任务完成边界漏检，不是车身碰撞，不能解释为初始状态无解。

### F2：九条feasible_witness不是统一完整约束下的九个见证

`independent_replay_check`没有独立重判纵向任务终点、全程动态余量或F/B环境规则，它依赖原success筛选；离线分段搜索没有最终s<=170检查，也缺少完整准备期验证。

两个离线候选ample_none_i1和ample_prepare_i1都是wait=2、a_M=0、a_R=-2，总时长5s，M终点172.5m，不能作为170m任务见证。保存见证中的B_ample_none_i2、A_ample_prepare_i2又继承F1漏检。B_ample_none_i2还有动态余量负值。

现有候选中，下列五个在已复核的任务完成与动态余量检查范围内得到支持；不是断言其他环境不可行：
- ample_none_i0，A保存轨迹；
- ample_prepare_i0，A保存轨迹；
- collaborative_none_i0，wait=1.5s，M/R均命令−2m/s²，含原jerk执行，4.5s结束，M=155.68125m；检查范围内最小动态余量约0.45625m；
- collaborative_prepare_i0，同上，最小动态余量约0.331635m；
- collaborative_none_i2，wait=2s，M/R均命令−2m/s²，5s结束，M=161.05625m；最小动态余量约0.00625m，属于近边界候选。

离线候选使用原F脚本与B闭环跟驰，不是固定回放B。离线候选目前只验证到机动完成，不能据此声称16s持续安全或连续时间保证。以上三个collaborative环境中，当前A–E全部失败，已经是需要解释的在线协调缺口。

### F3：257/458动态间距标记发生在SUCCESS_RELEASE，R没有真正释放旧任务

458条是配对—时间步标记，不是458次独立事故。按阶段：EXECUTE176、SUCCESS_RELEASE257、FAILURE_HANDLING25；按对：FM201、MR257，涉及8次运行。

v1只把M切为任务后巡航；R始终使用F速度及旧FR目标间隙误差，而实际前车已是M。257条成功后MR标记中249条R名义加速度仍为正。一次8运行的小型诊断仅将任务结束后的R改为跟随当前实际前车（复用现有IDM），257条成功后标记全部消失，其余201条保留，原完成/失败标签不变。这说明后处理会显著干扰整个16s成本，不能全部归因于耦合或jerk本身。

实际状态的`h=g−(2+0.35v_follower)`最小约−3.803m，11次运行有负h；原458标记还使用0.15m容差，二者不同。没有车身碰撞不等于全程动态余量满足，也不应把负h直接叫作真实事故。

### F4：间隙参考更新后，R空间反馈还使用旧导数系数

v1真实目标：`G_R(q)=G0+Delta_new*q`，`Delta_new=max(0,max(G0,21.4)−G0)`（本配置），但梯度仍乘`scenario.delta_g`。
若Phi_R=(g−G_R(q))²/(2*ell_g²)，正确的局部导数是
`−(g−G_R(q))*Delta_new/ell_g²`，不是乘旧delta_g。

| 情境 | Delta_new(m) | 原scenario.delta_g(m) |
|---|---:|---:|
| ample，全部 | 0 | 4 |
| collaborative，变体0/1 | 0.2 | 8 |
| collaborative，变体2 | 3.2 | 8 |
| short_window，变体0/1 | 8.2 | 7 |
| short_window，变体2 | 11.2 | 7 |

所以collaborative 0/1在非零误差处差40倍；ample真实R进程空间梯度为零，却仍被旧公式写成非零反馈。

更重要的模型问题：全部17次reported completed都在ample，该组dG/dq_R=0，R名义空间动作不受q_R影响。充足间隙不制造额外协作是合理的，但这些案例不能检验“R进程产生新间隙”的能力。把非空区间下限直接设成唯一控制目标，也不是“统一物理任务”的必然要求；安全可行集合与控制参考必须分清。

### F5：no_W和no_partner切断的是同一个动力学项

no_W把W设为0，而伙伴项是K*W*sin(...)。六个环境中它与no_partner的q、动作、轨迹及代价逐项相同。它不能单独识别空间依赖权重的作用。
应比较full、保持非零伙伴作用但冻结W为预先定义常数/初始权重、删除伙伴项；不要使用待评估完整轨迹的未来均值作为在线固定权重。

### F6：同快照没有恢复同一个内部状态

主日志没有e_M0，但同快照代码从日志读取e_M0、缺失即0；D的prev_k_mr平滑历史也回到初始值。C_ample_none_i0在1.25s实际e_M0=-7.8m、a_M=-0.211998，而同快照full输出-1.248842m/s²。该“full”不重现原动作，所以56行不能直接作为所声称的同状态干预。

应该在运行中深拷贝完整控制器状态，再从同一个副本起点分别干预；先验证无干预full复现原动作。若重建必须保存最少的必要历史，不要用默认零值代替缺失状态。

### F7：E参考生成仍混用候选时间与当前/下一步状态

`_online_target`用候选T算target_c，随后平滑T，却用旧target_c与平滑T再算a_req。`_reference`又把一拍后的`s+v*dt+0.5*a_req*dt²`当作当前参考来计算位置误差。

一个控制器完整链条自洽性探针：所有车18m/s、M在充足间隙中心、纵向速度已完全匹配，A–D输出a_M=0，E默认状态输出−5.956875m/s²（混用T导致a_req=−6）；令E内部T已等于最佳1.5s，则a_req=0，但未来位置对当前状态的误差又导致a_M=+0.225m/s²。零误差测试必须覆盖参考生成器到输出，而不仅是单独的跟踪函数。

## 三、第一步：一次性修正公共判据与任务后控制

1. 使用同一时刻的状态联合判定完整横移、170m终点、有限车身插入及道路条件。仍以现有坐标定义170m，不能偷偷从中心换成前保险杠或延长路段。离散步内同时发生事件时明确处理；边界附近可用更细时间步交叉核算，不把0.6m越界称作数值误差。
2. 任务完成后M/R都释放旧协作目标，按实际纵向/横向占用确定各自当前前车，进入共同、可解释的巡航/跟驰；F仍是原环境规则，B仍是原闭环响应。失败后的横向连续性与道路任务完成要分开，继续一条安全处置轨迹不意味着补记任务成功。
3. 明确三层结果：实际车身/道路、项目的动态余量条件、任务完成。h若是硬约束就在全过程统一使用；若是软目标则必须明确，不能为了通过见证临时改口或降低阈值。0.15m记录阈值与严格h>=0分开给出，不把它们混用。
4. 安全执行应针对jerk之后真正可执行的动作。至少先求每车可执行动作区间，再与相关安全约束共同处理，不要重复减小已被jerk裁剪掉的目标值。可以使用小型联合可行性/二次规划，也可以推导等价的一维区间处理；选择最简单的正确方案。F脚本不由优化器任意操控，B不变成额外协作智能体。用实际下一步速度计算下一步余量。
5. 一步约束有解不是全程安全证明；无解也不代表初始任务物理不可行。记录具体状态与失效原因，并改进提前预测和启动策略，不做事后位置修改。最大制动的最小前进距离只用于必要排除，不能被称作足够的启动计划。检查拟执行动作在3s期间是否存在可保持的占用方案，而不仅存下一个未使用的预测值。
6. 修正进程目标与梯度的一致性，有限差分在有代表性的内部q状态检查；保留q到1不等于成功的约定。

目标不是保证任何输入都能安全完成，而是对当前小矩阵给出一致模型、可解释失败与真实可执行控制。不要扩展成通用安全认证项目。

## 四、第二步：统一独立轨迹检查与可行候选

实现一个对主方法、保存轨迹和离线候选完全一致的检查器，不依赖控制器写入的success、collision计数或原feasible标签。

至少验证：初始状态、当前离散动力学与动作/jerk、F脚本及B闭环规则、准备期FR/RB、横向占用期相关车身与动态余量、横移连续且完成、终点170m和完成时插入位置。对任务后观测窗另给检查结果；到机动完成的见证不要宣称覆盖完整16s。每个见证保存动作序列、横移开始时刻、最小余量和主要状态，可独立重建，不只保存found=1。

保留三种可行性结论：找到可验证见证、确切必要条件排除、未判定。检查未通过应撤回“这个候选是见证”，不能推断“这个环境无解”。原18环境保留，不只筛D成功场景。

优先复核上述五个已有支持候选，对另外四条先修正标签、寻找有预算限制的替代候选；不必先把所有18环境彻底求解。尤其保护三个collaborative候选的具体可行任务意义。

## 五、第三步：把工作重心转回方法模型，而不是继续无目标修补

在共同基础上，对以下两个主例逐时间检查：
- collaborative_none_i0；
- collaborative_prepare_i0。
将collaborative_none_i2作为近边界对照，ample_none_i0作为无需额外开隙的负对照。

比较离线有效候选和A–E的在线动作，找到“首次丧失仍可完成机会”的原因：是M对齐时机、R开隙力度、固定最小目标、进程饱和/准备门槛、执行器修正，还是预测/任务选择。给出真实状态与动作，不只输出一串failure标签。

明确内部变量的实际控制能力：
- 在固定物理状态下，q_M变化如何改变M的参考位置/速度/动作？
- q_R变化如何改变R的目标和动作？
- K分配变化怎样经过q影响后续动作，而不是只有K日志不同？
- 当前任务是否根本不需要R主动开隙？此时应允许正常退化，不人为强迫协作。

若确认静态最小间隙参考或有限q组织方式阻止合理的瞬态合作，允许作**一次有明确数学与交通依据的模型修订**。例如，依据同一预测条件下的目标时空窗口生成可调整的间隙/对齐参考，或将内部进程推进与真实剩余可执行时间协调。具体形式由分析决定，不机械采用例子。不能为了让相位曲线有变化注入无交通意义的振荡。

必须区分物理约束和控制目标：所有方法面对同样道路、动作能力、环境、任务完成条件与信息；不要求每种方法必须追踪最小间隙这一条轨迹。若新的参考生成本身属于贡献，单独设对照，把它与双向性、非互易分开。不得给D独有更远观测、更宽道路、更强制动、更大搜索预算或无需解释的目标间隙。

本轮不要同时引入更多协作车辆、目标搜索、高维网络和强化学习。D可保留为待检验扩展；不能因非互易无优势就不停新增复杂模块。

## 六、第四步：用恰当干预识别机制

先修正同快照和W干预，再比较：
1. 无干预full（必须重现原控制器状态输出）；
2. 只关闭正确的空间梯度；
3. 以非零、预先选定常数W保留伙伴耦合；
4. 删除伙伴项；
5. D在同状态、同成对总增益下改为对称分配。

各干预独立从同一完整状态出发；q、e_M0、平滑增益、目标时间等不能默认重新初始化。短时动作影响与完整闭环效果分开。固定状态一次调用中没有差异不等于整条动态链路无差异；反之变量数值非零也不等于任务有效。

重要结构事实：令delta=q_M-q_R，在固定物理快照、同W、未裁剪且未终止时：
`delta_dot=b_M-b_R-(K_MR+K_RM)*W*sin(alpha*delta)`；
`qbar_dot=(b_M+b_R)/2+(K_RM-K_MR)*W*sin(alpha*delta)/2`。
固定总增益后，非互易分配不直接改变瞬时相位差收敛项，主要改变共同推进及角色分担；空间闭环和饱和后仍可产生差异。请据此检查作用机制，不把“同步更快”当成必然的非互易优势。

保留在线时机基线E，并修正其已确认的实现不一致：若平滑了候选T，就应对同一个T重新计算目标位置，不能用新T配旧端点。它仍是工程基线，不冒充文献复现。不要因为它某指标较好而削弱它。

## 七、实验预算与停止范围

- 原18环境是回归与探索集合，不再冒充未见测试集。
- 公共修正后重跑A–E原90矩阵，记录版本变化，完成数与代价不可跨物理版本直接排名。
- 在上述少量诊断环境做正确消融；统一且有限的调参预算，优先由作用时标/量纲指导，不对每个被测环境分别挑最优K。
- 至多开发一次核心模型修订，并完成对照；不得循环到D赢才停。需要改动研究主线时先报告。
- 通信扰动留到“有效检查器＋同状态干预＋有意义的名义作用链”成立后单独设计；不要求名义性能一定优于所有基线，但必须能解释通信通道及其退化会改变什么。
- 不只报告平均全车速差距离。同时给任务完成、声明约束满足、失败类型、M/R/F/B代价，以及准备/执行/任务后成本。失败的低成本不能单独解释为效率高；全样本与独立见证子集均保留。

## 八、应交付的实质成果

1. `docs/model_v2.md`：输入、内部状态、输出、空间—进程方程、非互易作用、实际执行与约束；明确保留/改动/失效的创新假设。
2. `reports/stage1c_report.md`：公共修正、模型修订、真实结果与下一步判断；不以测试通过代替科学内容。
3. 对原90矩阵的指标、事件、车辆代价，统一见证表与见证动作轨迹，正确同快照/闭环干预表。
4. 少量关键图：成功/失败时空窗口；q到实际动作的作用链；符合相同约束时的任务与全车代价。不要把位置(m)和加速度(m/s²)放在同一个未区分的纵轴，把动作细节压扁；终止后的原始反馈和有效反馈分开。
5. 必要回归测试：最后一步越过终点、伪见证172.5m被拒、目标不依赖q时真实梯度为0、同快照full重现、非零固定W不等于删伙伴、成功后R跟实际前车、jerk执行后约束检查。测试针对本轮问题，勿堆无关防御代码。
6. 自包含轻量`feedback/stage1c_feedback.zip`，包含运行依赖、配置、模型、报告、见证动作、关键轨迹、图和复现命令。全轨迹可留本地；不打包缓存、虚拟环境、密钥、论文全文。审阅包里的源码只供核对，不能直接覆盖用户代码。

最终答复必须写清：实际改了什么、跑了多少、哪些见证有效、哪些机制改变了实际动作、哪些结果支持或不支持当前创新。达到上述范围后停止扩大实验。

现在开始执行。
