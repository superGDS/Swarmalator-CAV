# Codex执行指令：Swarmalator–CAV研究启动与首轮闭环机制验证

请作为本项目的科研实现负责人，在当前选定的工作区中直接开展研究实现。本轮任务是“建立并运行首个可解释、可比较的闭环原型”，不是只撰写研究方案，也不是一次性完成整篇论文。

请先简述你的执行安排，然后继续阅读必要材料、检查数学模型、编写程序、运行测试和配对实验、检查图表与数据、修复发现的实现问题，最后交付可复现的结果包。本轮范围内的常规技术选择和修复由你自行判断，不必逐步请求批准。

## 一、研究目标与阶段边界

研究主题：面向有限汇入窗口的swarmalator启发式CAV协同控制，通过“间隙形成—机动进程”的双向耦合，以及角色之间的非互易响应，联合调整车辆的空间关系与机动时机。

需要检验的研究假设：
1. 当间隙形成与汇入车到达时机相互制约时，实际运动反馈到内部进程、内部进程再影响伙伴动作，可能改善协作的可执行性。
2. 在成对总耦合强度相近的条件下，依据车辆调整余量分配两个方向的影响，可能降低整体调整代价，而不是始终让某个角色牺牲。
3. 这种价值具有条件性；机会充足时可能不需要复杂协调，窗口过短时也可能无法补偿物理限制。

这些均为待检验假设。首轮不预设新方法优于基线，不要求证明双向耦合普遍必要，不宣称全球首创、全局最优或真实道路安全保证。程序正常运行、机制确实生效、交通表现改善，是三个不同结果。

整体路线：Python少车原型 → 机制深化与强基线比较 → SUMO混合交通 → 有需要时开展CARLA/通信部署验证 → 可选MARL学习低维协调参数。本轮只执行第一部分，并完成一个可运行的在线协调对照。暂不安装大型仿真平台，不训练MARL，不开展硬件控制或大规模参数搜索。

## 二、工作区和材料

先检查当前目录、已有文件、适用的AGENTS.md、Python环境与可用依赖。以实际工作区为准，不假设Windows/Linux、不猜测用户目录，不引入其他研究项目的路径或状态。保留已有改动；在工作区内使用独立环境，常规依赖可按现有权限安装，不修改系统环境或关闭权限保护。

优先阅读工作区或references目录内的两篇论文，文件名可能与标题不同，应核对PDF标题：
- Navigation of Robotic Swarmalators With Dynamics and Constraints，DOI: 10.1109/LRA.2025.3640404。重点检查第2页空间与相位模型、第3页分层执行、第6—8页约束验证及适用边界。
- A cooperative decision-making method for CAVs from the perspective of opinion dynamics，DOI: 10.1016/j.trc.2025.105412。重点检查方案—偏好—共识—执行的对应关系、运动反馈与实验比较，而不是完整复现其环岛算法。

需要时，再定向核查原始swarmalator、非互易机器人控制、Kuramoto车辆协调或虚拟时间协调的直接近邻。控制阅读范围，使其服务于当前模型与基线实现；不要重新扩展成全面综述。技术细节优先查作者、出版社或官方文档。

如果本地论文缺失，可在授权网络范围内寻找合法公开版本。仍不可得时，明确标注未读到原文；本指令给出的候选模型可以作为独立研究设计继续实现，但不得称为论文复现。只在缺失信息确实阻止核心任务时提出具体问题。不要假设能够访问其他聊天的附件、全文或本地路径。

将原文方法、我们提出的改造、你本轮必要的修正分别说明。尤其不要把已有观点动力学简单描述成没有运动反馈，也不要把类车机器人导航直接当作道路CAV验证。原论文中的机器人尺寸与参数不能直接作为交通标定值。

## 三、第一版实验对象与执行接口

建立一个CPU可运行的四车汇入环境：
- M：汇入CAV；R：目标间隙后方的协助CAV；本轮只对M/R实施新协调器。
- F：目标间隙前车，按统一、可配置的驾驶/扰动规则运动。
- B：R的后车，采用会响应前车运动的普通跟驰模型，反映协作外部影响；不把B强行设为合作成员。

固定单个目标间隙，先不做复杂伙伴搜索。道路有明确的汇入允许区和终止位置。使用统一道路坐标、SI单位、有限车长/车宽；初版可采用纵向动力学与连续横向轨迹，明确其简化，不冒充完整自行车动力学。

协调器统一接口可采用：
controller.step(local_observation, received_messages, task_state, dt)
返回名义运动指令、更新后的任务状态、消息和诊断量。

各方法采用相同的观测权限、预测模型、候选间隙、动作限制、横向执行规则、安全检查与评估时间。所有车辆根据同一个时刻的快照计算，再统一推进仿真。控制器只访问观测和已到达消息；不得读取未来扰动、未来真实轨迹或其他车辆未公开的控制决策。

第一轮采用理想但显式的消息交换即可，不必先构建无线网络。F的扰动脚本只属于环境；B闭环响应，不能固定回放一个不会受控制影响的未来轨迹。

## 四、候选模型：先检查再实现，不机械照抄

以下是研究原型，不是原论文公式或已经验证的最终模型。请检查量纲、导数符号、信息依赖、饱和与端点行为；发现问题时作最小必要修改，保留修改理由。不要为凑名称而添加没有实际作用的正弦项。

1. 状态与几何

车辆状态包括s、y、v、a以及车身尺寸。内部机动进程q_i∈[0,1]绑定当前任务，表示准备推进程度；它不是合作概率、横摆角，也不是物理换道完成比例。

物理横向换道进度单独表示。准备进程变慢不能使已经开始的横向运动突然倒退。任务开始、执行、完成、退出分别处理；q达到1不代表真实汇入成功，也不应强迫所有车辆q相同才允许一个本已安全可行的机动。

使用车身中心坐标时：
g_FR = s_F - s_R - (L_F + L_R)/2。

M可占用的纵向安全区间，可由以下边界定义：
lower = s_R + (L_R + L_M)/2 + d_RM；
upper = s_F - (L_F + L_M)/2 - d_MF。

其中d_RM、d_MF由所有方法共用的间距规则确定。当lower>upper时，当前区间为空；准备参考仍可定义，但不能宣称已经可执行。执行前还需检查机动持续期间的预测占用。

2. 进程驱动空间协调

令G0为初始净间隙，DeltaG为根据任务需求得到的非负间隙增量；初版可固定任务目标以便辨识，动态安全余量仍单独检查：
G_R(q_R) = G0 + DeltaG*q_R；
Phi_R = 0.5*((g_FR-G_R(q_R))/ell_g)^2。

令c(X)表示考虑车长和前后余量的目标位置，e_M0为任务开始时M相对该位置的偏移：
s_M_ref = c(X) + (1-q_M)*e_M0；
Phi_M = 0.5*((s_M-s_M_ref)/ell_s)^2。

可用如下结构生成名义纵向动作：
a_i_nom = a_i_ff - k_xi*partial(Phi_i)/partial(s_i) - k_vi*(v_i-v_i_ref)。

明确前馈与速度参考如何由可观测状态获得；求偏导时应说明固定哪些量，不忽略参考对状态的真实依赖。

3. 空间反馈到进程，并在车辆之间传播调整

候选更新式：
qdot_i = clip(nu_i - kappa_i*partial(Phi_i)/partial(q_i)
              + sum_j K_ij*W_ij(X)*sin(alpha*(q_j-q_i)),
              0, nu_i_max)。

W采用简单、有界的空间相关权重；alpha须使有限进程范围内的协调方向符合预期；到达任务终点后明确终止行为。检查“空间准备落后时减慢推进”是否真的成立，并记录名义推进、空间修正、伙伴作用与裁剪量。

特别检查e_M0=0、DeltaG=0、q到达终点、进程被长期裁剪等退化情形。出现退化不等于软件错误；应区分是否意味着此情境确实不需要某个通道，还是当前交通映射设计不足。

4. 非互易分配

统一约定K_ij表示i响应j的强度。一个候选实现为：
K_MR = K_total*sigmoid(z_M-z_R)；
K_RM = K_total-K_MR。

z_i表示与当前调整方向有关的能力和代价，使用可解释的加减速余量、剩余距离及前后车约束估计；不能读取未来真实响应。K_MR较大表示M承担更多跟随调整，不表示M更有支配权。

先实现对称与状态相关非对称两种分配，保持同一快照下成对总增益一致；对权重变化作适当限速，避免抖动。固定非对称可作为额外诊断，不必首轮扩展很多版本。

上述候选可简化或修正，但必须保留：独立内部进程、进程对相对空间运动的实际作用、实际空间对进程的反馈，以及可解释的方向性调整分配。若最终只能解释为普通虚拟时间跟踪器，应如实指出，并提出下一轮最小区分实验，不强贴swarmalator标签。

## 五、共同的执行、安全与成功判定

先采用一套所有方法共用的加速度/jerk受限执行器，必要时使用小型QP或短时域跟踪MPC。采用最简单且能明确说明的实现，不为了工程完整性引入多个求解框架。执行器主要跟踪与安全修正，不暗中重新选择间隙或替上层完成全部联合规划。

保存a_nom、执行器输出、实际a以及每次裁剪/安全修正的原因。安全检查覆盖有限车身、主线前后关系、横向进入期间的双车道占用与道路终点；用适当子步或连续近似检查步间碰撞。

遇到不可行安全约束或数值问题，记录真实状态与处置；不能偷偷放松硬约束、搬移车辆、删车或把失败变成成功。这里是受控仿真研究，不宣称获得了真实道路安全保证。

成功按实际车身在允许区域内完成汇入、且未发生预定的物理约束违例定义。区分完成、未开始而错失窗口、开始后失败、超时和数值错误。全部方法采用共同观察窗口，任务退出不能提前截断交通代价统计。

## 六、完成一轮小规模、可解释的配对比较

至少实现并运行五种配置：
A. 普通空间间隙/速度协调，不使用内部进程。
B. 单向进程驱动：进程影响空间，但移除新增的实时空间→进程反馈。
C. 双向耦合，M/R相位作用对称。
D. 双向耦合，采用能力相关非互易分配。
E. 一个可运行的在线间隙—到达时间协调器：依据当前状态滚动调整目标时机，具有同等信息和动作约束，不是固定时钟或不响应扰动的弱对照。

E可采用小规模候选时机搜索/虚拟时间反馈/简化MPC，由你选择并解释。若未严格复现某篇论文，明确称为本项目实现的工程基线。首轮不要求实现文献中的完整联合最优控制；后续再加强竞争基线。

保持共同任务目标与正常安全反馈。B的消融必须检查K、W、nu、目标更新等旁路，不能删掉一项后仍由另一项恢复同样通道。固定任务片段中先检查依赖关系；正常任务完成与安全终止不应被删除。

建立三类初始条件：现有间隙较充足、间隙需通过协作调整、短窗口/强约束。第三类不能仅因某个算法失败就标为“物理不可行”；无必要条件证明或可行轨迹验证时应标为可行性未确定。

每类包含无突发扰动及准备期扰动两种情况。用真实变化的初始状态而不是仅修改无作用的随机种子。建议从“5方法×3情境×2扰动条件×3初始状态=90次评估”起步；这是规模建议，可根据实测耗时缩减，但应保留全部比较维度并报告实际数量。

调参用单独的少量开发情境，给予各方法相近预算；配对评估使用另设场景。首轮均为先导研究，不包装成正式确认性检验。评估后若修正模型/参数，应标记为新探索版本，不能保留“未接触测试集”的说法。

不要无限搜索直到新方法获胜。实现错误应修复并重跑受影响比较；假设不受支持时也要交付真实结果。碰撞、超时和无收益情境不能从汇总中删除。

重点检查：量纲与方向、数值积分与步长敏感性、车身与成功判定、同快照更新、成对总增益、通道关闭、随机可复现和失败被正确计数。测试用于确认实现，不要求所有研究情境都成功。

## 七、程序组织与产物

使用Python，优先NumPy/SciPy/Matplotlib和轻量配置。依赖按需增加。代码按状态/环境、协调器、执行安全、基线、运行分析等职责划分即可；避免提前建立大量空模块、服务、兼容层、复杂运行认证与无关技能链。

为后续继续工作保留简短AGENTS.md（若已有则只作必要增补）：研究范围、真实证据原则、常用命令和当前报告位置。不要把本长指令全文复制成每轮都强制阅读的规则。

实际创建并核验以下产物，路径可因现有项目结构作合理调整：
- README.md：本项目做什么、依赖、真实可运行命令、阶段路线与当前状态。
- docs/model_v0.md：实际实现的方程、变量单位、信息来源、理论来源与本轮改造、关键假设；必须与代码一致。
- configs/：场景、方法参数及本轮实际采用的展开配置。
- src/与tests/：可运行实现和关键测试。
- outputs/stage1/：每次运行的指标表、轨迹、任务事件、运行与测试日志。
- reports/stage1_report.md：结果、解释、问题与下一步建议。
- feedback/stage1_feedback.zip：供我返回研究讨论窗口的精简反馈包。

逐次日志至少包含：时间、方法、场景与车辆标识、位置/速度/加速度、q和更新分项、K和W、实际与目标间隙、任务阶段、动作修正与失败原因。单位和统计口径写清楚。

汇总至少包含：完成率/失败类型、全部四辆车的调整代价或延误、最小净距与约束违例、jerk、安全修正频率/幅度、计算耗时。若用固定时间窗的速度缺口积分作为代价，要这样命名，不冒充实际旅行时间延误；未完成任务不赋零延误。

生成三组解释性图：
1. 代表场景的空间轨迹/间隙、任务进程、名义与实际动作，标出扰动与执行时刻；
2. 各方法在不同情境下的机动结果和整体代价；
3. 非互易分配、角色调整代价及安全修正之间的关系。

图必须从实际保存的数据生成；展示有利、无差异及不利结果中实际存在的类型。先检查图表是否与CSV一致。无需先制作精美动画、PPT或投稿手稿。

反馈包包含README、模型说明、核心源码/测试/配置、报告、指标、关键轨迹与图、必要日志；排除虚拟环境、缓存、无关文件、原始论文全文和任何密钥。包内保留相对路径和运行入口，能够定位结果对应的代码与配置。

## 八、完成本轮再交接

本轮完成意味着：程序已实际运行；关键测试和失败情况已检查；配对实验及图表由真实输出生成；报告能解释方法差异；反馈包已存在。仅创建文件结构、写完代码但未运行，或只列出下一步计划，都不算完成。

最终回复按以下顺序：
1. 当前实际实现了什么模型，和候选式相比作了哪些必要修改；
2. 实际运行了什么测试和多少条实验，结果的具体数值；
3. 空间—进程反馈与非互易分配是否在轨迹中产生了可辨识作用；
4. 哪些收益受到安全层、基线、调参、目标定义或简化物理的影响；
5. 真实文件路径、复现命令和反馈包位置；
6. 下一轮最值得做的一项研究工作及其理由。

不要为了表面积极而掩盖零结果，也不要因初版效果不佳就直接否定整个研究。分别判断实现问题、模型映射问题、场景不敏感和当前假设确实缺少支持。

完成上述首轮闭环后停止扩大实验，等待我带回结果讨论下一步。只有遇到真实权限/资源阻断或必须由用户作出的重大选择时提前交接，并保留已完成产物和具体阻断信息。
