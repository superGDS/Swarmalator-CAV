# Codex执行指令：Stage2A——从有限进程原型转向预测可执行的空间—机动协调

## 0. 直接执行的任务

继续当前 `F:\Swarmalator–CAV` 项目。先完整阅读适用的AGENTS.md、Stage1C模型/代码/结果，再完成本轮模型设计、程序实现、测试、有限闭环比较、图表和反馈包。不要只返回计划，也不要继续用一轮轮局部补偿替代方法研究。

本轮目标是：**让在线控制器从当前允许观测出发，生成确实可执行的M/R联合准备；在此基础上识别空间—内部进程耦合与非互易分配的独立价值。**

这不是要求D获胜，也不是要求所有环境成功。优先分析和解决已知有可行轨迹、当前协调器仍失败的环境。失败、零增量和有代价的改进均保留。不要把真实的不利结果改写成优势。

原Stage1/1B/1C证据与输出均冻结保留。本轮使用新的模型版本、运行入口和 `outputs/stage2a/`。可复用已核验组件，不要再完整复制整个模拟器产生多层不一致逻辑，也不要为了去重开展大规模重构。新方法与公共修正需要清楚分开。

本文件包含全部必要复核信息，不依赖外部审阅包存在。审阅包缺失时继续工作；实际源代码与文字有差异时，以本地可验证代码为准并记录。

## 1. 研究定位与边界

对象：四车汇入。M请求汇入，R为间隙后车，F为动态前边界，B闭环跟驰并体现外部代价。现阶段控制M/R，不增加车辆/道路类型，不进入SUMO、CARLA、硬件、MARL或通信延迟/噪声。

核心假设仍是：实际空间条件能够改变内部机动协调状态，该状态反过来改变相关车辆的间隙与时机调整。非互易性是第二层问题：不同角色如何分担调整，而不是“非对称K一定提高成功率”。

之前的有限q+即时平方误差反馈是候选工程原型，不是必须维护的最终模型。本轮允许一次有明确交通/数学依据的结构重构，但不能悄悄更换主题。重构前先用现有证据明确：要补足的是动作表达能力、预测/决策能力还是执行可行性。

不得宣称世界首创或普遍必要性。原机器人论文是“swarmalator规划—约束修正—底层跟踪”，并未要求相位独自完成所有任务推理；观点动力学论文是先生成可行联合方案，再协调偏好、执行滚动控制。可以借鉴这种职责划分，但不能把一个已经由优化器决定的完整轨迹贴上q标签后称为swarmalator贡献。

## 2. Stage1C已知事实：先核对，避免重复走弯路

提交ZIP的SHA256为 `c82a884272e6e41b95a42d6b68f578a64e950db51425a301dc449a9efcce9fd8`。独立复跑90主实验、36闭环干预、20条额外opening-off实验（与20条复用on结果组成40行）、18可行性诊断和60行同状态结果，均复现；17测试通过。这里列hash仅用于识别版本，不要求建设证书/审计系统。

### 2.1 完成口径

Stage1C事件完成：A/B/C/D/E各3/18，共15。
按独立检查的动态余量h>=0、完整动作/状态转移、170m终点及横移条件：A=3、B=1、C=1、D=1、E=3，共9条任务有效完成；这9条也覆盖16s。

B/C/D的`ample_prepare_i0`最小h约-0.09m，`ample_prepare_i2`约-1.26至-1.32m。没有硬车身重叠不等于h>=0。0.15m容差不能用于把严格违规改写成满足约束。

九个环境的保存见证在独立离散检查中仍成立：六个ample，以及collaborative none/0、prepare/0、none/2。前三类数字“9个环境、9条主运行、3条代表性完整窗口见证”对象不同，不得混淆。未覆盖16s不等于任务段无效。

`min_h_mission_m=0.088`是净距减动态要求后的余量，不是0.088m车身净距。日志/图表必须区分 `net_clearance_m` 和 `dynamic_margin_h_m`。

### 2.2 在线方法与见证之间的具体差距

collaborative none/0及prepare/0，wait=1.5s、M/R请求-2m/s²的候选，在jerk约束下可以在155.681m完成；none/2另有在161.056m完成的任务候选，Stage1C的A/E本身也能完成none/2。不要再写成所有collaborative环境A–E全部失败。

C/D在collaborative环境没有完成。在none/0，G0=21.2m、Gfinal=21.4m，q_R的全范围只能改变目标间隙0.2m。

R参考未裁剪时：
`a_R_nom=(v_F-v_R)+0.44*(g_FR-G0-Delta*q_R)+a_open`。
因此固定物理快照，q_R在[0,1]内只能改变R动作0.088m/s²。真实C运行t=1.5s，无论q如何选，R名义动作约在[+0.282,+0.370]m/s²，而可行候选此阶段请求-2。此结论不证明-2是唯一方案，但表明仅搜索q/K无法表达这个有用的联合准备。

**不要把最小几何间隙当成唯一瞬态目标。**不同方法必须共享安全/任务条件，不必锁死为同一条缺乏必要瞬态调整的参考曲线。

### 2.3 q终点与参考导数

`s_ref=c+(1-q)e0`，`v_ref=c_dot-qdot*e0`。q达到1时qdot硬清零，参考速度中的一项突然消失。54条B/C/D运行都在活动阶段发生该跳变。ample中被移除的参考速度分量约3.26–4.68m/s，collaborative约1.84–3.24m/s。

例如C collaborative none/0在2.4s q_M已经饱和、M约124.91m、插入区间仍为负。不能继续依赖一个已失去自由度的进程状态，声称它能在执行限制下持续组织协作。需要平滑的参考及端点处理，或重新定义与有限任务更相容的内部状态。

### 2.4 已写的预测没有进入决策

`occupancy_plan_check`定义了三秒名义预测，但现有代码没有调用它。37个实际启动快照上，该预测会预警全部22次started_but_failed（14动作区间不可行，8越过终点），还会预警15次事件完成中的3次。

这不是闭环成功改善实验：拒绝启动可能只把错误变成missed_window。真正要解决的是准备阶段怎样提前改变计划。预测不得访问未来F扰动脚本；名义预测通过也不是鲁棒安全保证。

### 2.5 检查器与打包的必要修正

现有`independent_trajectory_check`把动力学/动作错误写入errors，但mission_ok只检查mission_errors。合成测试把一条有效轨迹某步动作改成100m/s²，仍返回mission_ok=1且列出错误。修复按发生时间把所有约束/动力学错误计入任务有效性。此为合成测试，不是实际运行出现100m/s²；不能据此撤销已独立通过的真实见证。

反馈包的初始化和旧测试仍依赖simulation.py与stage1b_simulation.py，但包未包含。下一次包含实际需要的全部导入文件，或让本轮入口完全独立；不要要求研究窗口猜测缺失代码。

## 3. 本轮主流程

### 步骤A：一次性收敛公共执行/评价基础

1. 使用统一验证器给出三种标签：事件/几何完成、满足声明动态约束的任务完成、完整16s满足约束的完成。边界170m、道路与横向占用定义维持不变。
2. 按任务时间段统一纳入初始状态、速度、加速度、jerk、动力学、间距与完成边界错误；保留原输出，不回写旧版本标签。
3. 保留Stage1C的实际前车跟驰释放和E参考时间修正。安全层使用真正可执行动作范围；若动作集合为空，记录并采用明确的共同异常处理，不能标成“投影保证安全”。
4. 修正或替代带硬导数跳变的参考。所有被比较方法使用同等连续性、动作边界和安全约束。明确这是共同接口修订，非D独有创新。
5. 仅做保护研究结论所需的单测：被损坏动作不通过任务见证、边界完成步、参考端点连续性、实际前车释放、同状态full重现、物理预测与执行一致性。不要为测试数量而增加无效测试。

完成这些后，不再开展全项目完美性审计，进入模型工作。

### 步骤B：建立能实际生成可执行联合准备的预测基线P

目标：从当前合法观测在线生成 M/R 的准备动作与启动时刻，而不是读入离线见证表、环境ID或已知未来扰动。

可采用固定预算的短时域联合shooting/少量分段控制搜索，或小型联合MPC；优先选透明、可以维护且易于核验的方案。本轮不要求复现某篇文献算法，不把工程基线写成文献原算法。

建议第一版从已有少量动作/等待候选开始：预测完整准备段+固定3s横移+短任务后衔接，物理执行步仍0.05s；规划可较低频刷新。候选域与预算全方法公开一致。若选控制块参数，保留“较早、安全、持续的M/R准备”这一类动作，而不是只调当前瞬时加速度。

每个候选必须经同一动作边界、jerk和几何/动态余量模型向前传播。预测中B闭环响应R；F预测只能使用当前/过去信息和说明的预测模型。模拟器执行时继续采用真实F环境扰动；不得把disturbance名称、未来脚本、成功标签或场景初始变体号传入决策器。

候选评价应先区分可完成与未找到可完成，再在有效候选内比较全车代价和控制平滑性。不要用任意低代价的“从不执行”赢过成功方案。无可行候选意味着当前有限搜索未找到，不代表物理不可行。

决策必须避免滚动推迟：保存绝对计划时刻或显式剩余任务状态，复评旧方案时给它正常的连续性处理，不要每步把“再等1.5s”重置，造成永远等待。

找到可行预测后执行第一步，随后重估。若扰动使计划失效，应报告实际采取的重规划/保持/退出，不得静默更换为已知离线见证。

先在collaborative none/0验证P确实从合法观测生成了有效准备，再在prepare/0及none/2检查；保留两个ample作为无需额外合作的对照。若P仍失败，优先解释预测与执行/搜索表达差异，不进入相位学习。

### 步骤C：对空间—内部进程模型作一次结构重构

本轮不是在旧模型上继续叠加a_open2、a_open3、例外表或按场景硬编码。先写清“当前内部状态可以选择的空间动作集合”，再确定新参考族及内部协调状态。

必须满足：
- 允许表达有物理代价但有价值的瞬态间隙扩张/保持/释放，不将最低允许间隙误作永远唯一的吸引点。
- 机动阶段不同角色无需取相同物理动作。明确进程/相位的量纲、交通含义、可调整方向、与实际横向进度的区别。
- 相位到空间作用和空间到相位作用都是真实信息路径；不是只在结果中算相位。
- 有限任务结束时没有人为参考速度/加速度突跳。可用平滑进程映射、带终端导数条件的参考，或以时间窗口相关内部状态替代硬增长q，但必须解释改动后与swarmalator的联系和区别。
- 预测给出可执行性和任务方向，内部状态实际组织角色间空间/时序关系。不能由全局预测器先决定完整联合轨迹，再加一个不影响动作的相位模块。

一个可考虑但不强制照抄的建模起点是：用有限预测窗内的间隙、相对位置、终点/时限和执行余量构造任务势函数 `V_H(X,z)`，用内部协调状态z改变可执行的空间参考，反过来用预测任务误差修正z，再保留有明确定义的伙伴作用。不要只最小化当前平方误差。

这里V_H的梯度/有限差分必须针对真正发送给执行器的参考族。约束与候选切换导致不光滑时，采用能解释的离散更新或有限差分，不虚构光滑性、全局收敛或可达集证明。

非互易性只在对称模型具备真实空间—进程作用之后加入。固定共同状态和成对总K时，原两车相位差方程不受K方向拆分直接改变；它影响共同推进节奏和角色分担。评估应围绕任务余量、谁做了多少调整、外部车辆代价，而不是声称必然更快同步。

允许在先导环境上作一次有限修订，不允许基于全矩阵结果持续改公式直到胜出。记录每次修订的作用位置与预期证据，不需要长工作日志。

### 步骤D：预测收益、进程收益与非互易收益分开比较

建议控制在以下四类新方法，加一个必要的旧模型回归对照：

- P：具有同等预测、动作和安全条件、无内部相位的联合空间—时序基线。
- S1：新的参考族与预测条件下，内部进程驱动空间，但关闭新增空间→内部状态反馈。
- S2：同一新参考族、双向空间—进程协调、对称伙伴作用。
- S3：在S2基础上改变有明确定义的非互易分配。
- 旧C：仅用作跨结构诊断；若公共执行基础已改变，明确命名“旧C协调律＋新公共层”，不能当原Stage1C复现。

非线性求解/预测预算、观测权限、执行器、道路、目标机动、车辆限制保持可比。P不能被故意设计成更弱的瞬时规则。阶段内至少有一个相同参考族下不使用伙伴作用的对照，以免“新参考族更好”被误写为“群体耦合更好”。可在少数诊断场景做，不必再完整膨胀主矩阵。

如果P已经得到全部收益而S2/S3没有额外增量，报告应明确增益来自预测规划。可以保留模型解释、计算节省或特定任务能力的真实结果，但不能临时换指标包装成优越性。

## 4. 实验范围与评价

保留原18环境作为已经多次使用的开发/回归集，不能宣称它们是未见测试集。

先完成5个锚点：collaborative none/0、prepare/0、none/2，ample none/0、prepare/0。在这些情境中形成有解释的闭环后，再跑原18环境的新方法矩阵。无需把所有unresolved先判定完才能推进。

采用固定、说明的有限设计/调参预算，不对每个环境单独挑参数。必要时加入至多一小组预先声明的轻微初始状态变化，专门排除硬编码，仅称开发稳健性检查。正式泛化测试留给下一阶段。

核心输出：
1. 任务有效完成、几何完成、完整窗口有效完成；所有失败类型。
2. 当前状态首次提出可行计划的时间、实际启动/完成时刻、首次失去可执行性的原因。
3. 间隙/相对位置/内部进程/名义与实际动作的闭环链条。
4. 全车速差距离（m），按准备、执行、成功释放、失败处置分解；同时报所有任务与共同有效完成任务，给出分母。
5. 动态余量、真实车身净距、硬碰撞/道路条件；动作饱和与安全约束修正分开。
6. 单次决策计算时间及候选数，包含在线预测开销；仿真跑得快不等于每个决策满足实时。

不能把空间余量的h最小值当车身净距；不能把饱和次数当独立安全事件；不能把失败后不再服务任务造成的低成本称作交通效率优势。

## 5. 关键实现建议

将主改动集中在：
- 一份可复用的物理预测/任务检查代码；
- 预测基线和新的协调模型；
- 一个Stage2A运行器与少量针对性测试。

预测模块返回候选动作/参考、启动时间、预测完成位置、最小h、执行余量、计算成本与不可行原因。控制模块不得通过模拟器对象访问未授权状态。

无干预重放先在同状态下复现原输出，再做相位、伙伴或K干预。不要将“设置W=0”和“删除伙伴项”当成两个独立因素；要检验状态依赖W，用匹配的固定非零W。

如果控制器保存计划、预测轨迹或上一解，完整状态克隆必须包含这些信息；禁止同快照试验悄悄重置规划器历史。

模型说明要写出真实运行公式与伪代码，不要只解释理论名称。原型采用工程局部集中的联合预测时如实标明；没有独立进程/消息约束验证之前，不宣称已实现完全分布式部署。

## 6. 权限、交付与停止条件

在现有权限和工作区内，可以独立编写代码、安装必要轻量依赖、运行测试、作有限调参与修正。仅在真实权限阻断、不可恢复覆盖、额外付费或必须改变主研究目标时询问。

交付控制在清楚可用的文件：
- `docs/model_v3.md`：真正的模型、输入输出、与swarmalator联系、与普通预测规划的区别。
- `reports/stage2a_report.md`：核心结果、预测收益/耦合收益/非互易收益的分离、边界和后续建议。
- 本轮核心源代码、所需全部导入依赖、测试与配置。
- 主指标、统一验证结果、有限干预、关键动作/状态/参考与任务事件。
- 至少一条合法在线生成的协作成功轨迹；若没有，给出准确的失败状态与原因，不伪造、不无限扩大搜索。
- 三组有论证职责的图：可执行计划生成、空间—进程—动作链条、任务有效完成与代价/计算权衡。
- 一个可直接复现的命令。

生成 `feedback/stage2a_feedback.zip`。包含所有真正需要的导入文件，不包含虚拟环境、缓存、密钥或论文全文；完整大轨迹可留在工作区，但关键轨迹与动作必须足够独立重建。zip检查即可，不要另建复杂认证体系。

达到有限闭环交付后停止本轮扩大。最终报告首先回答：
**在线方法现在是否找到了原来错过的可行协作？改善来自预测/参考重构，还是确实来自空间—进程/非互易机制？下一阶段哪个贡献值得继续？**

现在开始执行。
