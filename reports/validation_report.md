# Validation report

Automated tests cover geometry signs, q saturation and terminal freezing, equal total C/D gain, same-snapshot matrix dimensions, jerk limiting, and a short run with finite outputs. The final command completed with `4 passed`.

The full Stage 1 matrix contains 90 runs and 32 physical completions. It records the saved failure outcomes without filtering. The aggregate hard body-overlap/road-exit count is 0; the minimum observed net gap over the matrix is 7.041 m. The three PNG figures were reread from the saved CSV tables; the compact key trajectory subset contains the representative D disturbed run, an A ample run, and an E collaborative run.

This is a software/prototype validation report. It is not a calibration or real-road safety validation.
