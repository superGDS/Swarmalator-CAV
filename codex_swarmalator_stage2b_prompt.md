# Codex 执行指令：Swarmalator–CAV Stage2B

## 本轮目标

继续当前 `F:\Swarmalator–CAV` 项目。不要重启选题，不再从零搭建模拟器。保留已经找回collaborative任务的有限预测器与候选参考族，完成**正确对照、可执行安全交接和独立耦合增量辨识**。

本轮核心问题：在相同合法信息、物理条件和公共执行层下，空间—内部状态反馈及非互易分配能否通过形成更适合后续跟驰接管的完成状态，减少额外制动与交通代价？公共安全修复不是S3专属贡献。

这是执行授权，不是仅写计划。你可以在现有权限和工作区内检查代码、作必要修订、运行测试和小规模实验、生成图表报告。不要每一步都请求确认。仅在真实权限、额外付费、重要文件覆盖或改变科学任务的重大选择上询问。

## 已有状态与保护范围

首先读取实际AGENTS.md、README、docs/model_v3.md、Stage2A报告、配置及相关源代码。检查本地版本是否与下述事实一致，不猜路径或未落盘结果。可选复核包不在本地也不阻断：本文件已经包含必要事实。

Stage2A反馈包为33文件，SHA256：
`6479ca93c7927a97aeb9e2125d561ee35f8a76bfc871e88b9d7697e68e5c1ea3`。

保留Stage1/1B/1C/2A反馈包和输出，不覆盖旧CSV或改旧标签。公共代码修改要使旧版本仍可根据归档复现；没有必要复制整套历史工程或增设繁重认证系统。本轮使用新版本号和outputs/stage2b。

## 一、必须核对的Stage2A事实

外部独立复跑90主实验、6闭环消融，非耗时结果一致，包内5项测试通过。这说明计算可复现，不意味着全部方法解释正确。

1. **S1与规格不一致。** stage2a_planning.py中partner只对P/no_partner清零；S1只将feedback清零，仍保留对称伙伴项和依赖空间的W。原S1不是“无反馈、无伙伴”。原204个S1决策中160个伙伴项非零，涉及16条运行。S2/no_feedback的两条锚点轨迹与原S1相同。仅用已有no_partner开关关闭S1伙伴项的18次诊断为任务有效16、完整窗口有效10、平均成本269.917 m；原S1为15、11、235.808 m。这只是规格干预，不是重新调参后的排名。

2. **成本分层改变解释。** 原S3−S2在全部18环境平均成本−7.813 m，其中释放段−7.492 m；在共同完整有效的11环境平均为+1.526 m（7个较低、4个较高）。原S2−原S1在11个共同完整有效环境平均−18.046 m且均降低，但这里仅新增梯度反馈。P与S3共同完整有效的7环境中，P成本全部较低，S3平均高37.202 m。保留数量增量与成本权衡，不能只选一种分母。

3. **存在硬执行问题。** 原主矩阵中P8条、S1/S2/S3各2条、oldC1条出现超过2.5 m/s³的jerk；最大80 m/s³。低速下速度非负与当前加速度/jerk区间冲突时，physical_step标记不可行，却取下界输出，导致a从−4瞬间变0。P_collaborative_prepare_i2在8.45 s出现MR矩形车身重叠，最大约0.111 m；oldC_short_window_prepare_i0在7.15 s出现约1.256 m重叠。它们多发生于任务后/失败处置，不应改写成原任务段必然无效，但全窗不能称安全。

4. **交接可行性缺失不全是F扰动。** P_collaborative_none_i0在4.5 s有效完成，M≈14.111 m/s、R≈16.093 m/s；5.15 s一步动作集合为空，5.20 s动态余量为负。F没有突发扰动。现有0.5 s尾段未覆盖该失效。

5. **预测与执行释放接口不同。** Predictor.evaluate在post阶段把post_targets经reference_command生成名义动作；run_episode在POST_PHASES直接覆盖为post_targets。相同释放状态的名义动作可不同，尽管共同jerk限幅可能暂时使实际动作一样。外部4组冻结候选延伸2 s时没有观察到实质轨迹差异，故不能预先把所有差异归于此处；仍应统一接口，特别是扩展后续预测以前。

6. **统计和验证汇总有小缺口。** run_stage2a.py用actual!=nominal作为安全修正，混入普通动作限幅。physical_step已有actuator/saturation/safety_correction，应直接使用。stage2a_check.py的out.update(ref)覆盖物理error字段；分开保存物理与参考错误。oldC计算了连续参考但未将其输出用于名义控制，应准确称旧律＋公共执行器，而不声称所有参考路径相同。

7. **变量含义必须正确。** v3的z是制动准备强度，a_target=−3z，不是机动进程或周期相位。P也持有计划、z、参考历史和梯度提议；区别是不采用新增漂移/伙伴演化律，不是无记忆。当前为联合预测实现，不是已实现分散通信。可以保留swarmalator-inspired研究定位，但不要把原振子稳定性、相位同步或群体自组织直接当作已验证结果。

## 二、完成一轮公共层收敛，不再追加一串局部补偿

### 2.1 实际动作和名义参考使用同一个明确接口

统一准备、执行、交接、正常跟驰的控制路径，使预测和实际调用相同的目标生成、参考/跟踪、执行函数。外生F只有预测模型与实际扰动不同；B保持相同的闭环模型。

冻结候选、冻结策略状态、使用相同F输入序列时，从相同初始状态滚动应当一致。比较s/v/a、横向进度、任务阶段和动作，不只比较函数名或数组形状。将检查延伸到任务完成后的足够时长，以暴露交接差异；不允许只检查尚未发生分叉的前半段。

不要用位置搬移、速度直接设定或重置物理加速度实现连续性。未被实际使用的参考轨迹不作为执行连续性证据。所有参考单位明确，图中不同单位分轴或分图。

### 2.2 处理停止可执行性和链式约束

区分：执行器自身可行区间为空，和多车动态安全约束交集为空。若lo>hi，不能继续称输出满足全部动作约束。不要用负速度、瞬时a归零、放大jerk上限或修改验证容差掩盖问题。

应在进入无可行动作状态以前开始减弱制动/调整同伴。连续时间示意：a<0时以最大正jerk J释放制动，到a=0仍需消耗约a²/(2J)的速度余量；实际程序须使用与dt/执行模型一致的离散判断，不把该简式直接当完整多车安全定理。

已有“一处约束空集，整条链全部最大制动”的备用可能恶化后车追近。选择一个可解释、执行一致的备用方案，并清楚记录约束仍无法保持的情形。不要为所有任意外界扰动证明安全，不要求完整高维可达集。

任务已经横移时不能冻结横向进度；失败后的运动仍连续。无法保证声明硬约束的运行继续明确无效，不冒充可行见证。

### 2.3 将完成后的可接管性纳入当前协作决策

几何完成不等于立即释放协调。预测/选择应考虑完成时的间距、相对速度、相对加速度、jerk余量及B的响应。将相同的交接控制与评价条件用于P/S1/S2/S3。

可采用共同的有限交接控制加终端条件/备份轨迹重放；不要只是把0.5 s任意延长一点并宣布递归可行。先基于失效时间和制动释放时标说明预测长度/终端指标为何适当，再保持一致比较。完整16秒仍保留为验证窗口。

不要把公共交接设计写成S3创新。若这些改进消除了此前代价差，按真实结果归因。

F扰动问题在以上一致性建立后处理：可使用当前可观测加速度、声明的有界预测族或有限备选，所有方法共享；不得读取未来脚本发生时间、场景名称、见证答案。扰动假设应单独记录，不以放宽安全间距换通过率。

## 三、建立正确、尽可能少的控制臂

本轮主矩阵只用P、S1、S2、S3：

- P：联合预测/候选选择基线，不采用新增漂移/伙伴状态演化律；保留其合理的最优候选选择能力。
- S1：内在准备强度演化/公共参考，但新增空间梯度项和伙伴项均为0。它仍使用共同预测可行性筛选，不要称它完全不看环境。
- S2：同一结构增加空间/预测任务势反馈和对称伙伴作用。
- S3：与S2相同，只改变方向分配，保持成对总K相同。

开关与日志逐项对应，测试S1两个项确实为0；S3设成对称后，在同一完整状态和接受规则下应与S2一致。

如优化器的候选接受规则、候选状态历史、平滑方式不同，要把整体方法比较与单模块干预分开。P选择最佳候选、S接受一个可行提议可以作为算法设计差异，但不能把因此产生的全部差值只归于正弦伙伴项。

记录原始提议、接受/拒绝、实际z变化、重新搜索后z重置。非零原始伙伴数值被拒绝时，不视为已施加的控制贡献。

W固定对照用非零常数，删除伙伴用0；固定值预先声明，不按每条环境挑最佳值。同状态干预克隆全部控制器与参考状态。

不要强行调大K使S3取胜。当前S3只实现特定后向余量驱动的启发式，不应表述为普遍最优能力分配。可说明方向设计的交通理由，但未经试验不扩张作用范围。

## 四、有限实验顺序与预算

### 4.1 公共层的少量判别检查

先在以下情形复核预测/执行一致性、停止边界、任务交接：
P_collaborative_none_i0、P_collaborative_prepare_i2、short_window_none_i1，以及一个ample_none_i0控制场景。

目的不是在每个极端环境中强行成功，而是明确状态是否合法、预测与执行是否一致、失败如何发生。到此收敛公共代码，不要新增大型模块。

### 4.2 五个锚点

collaborative none/0、prepare/0、none/2；ample none/0、prepare/0。四方法共20条，这些结果复用进入完整矩阵，不重复算为独立样本。

### 4.3 原18环境回归

四方法×18环境=72条主运行，包含锚点20条。oldC只需保留少量诊断，不再强制把18条不相称的旧结构当主要性能基线。

### 4.4 定向机制干预

在预先选定的2–3个代表性环境上，检查S2/no_partner、S2/no_feedback、S2/fixed_W以及S3/symmetric_K，约8–12条闭环干预即可。同状态检查不扩张大矩阵。

可增加一个简单有界线性/低通协调的锚点对照，回答正弦伙伴作用是否仅相当于普通平滑。它用于理解机制，不要求证明正弦对所有函数不可替代，不为维护名称人为加入振荡。

本轮计划保留的闭环实验约84–96条即可；必要修复重跑另如实记数。不要增加随机大扫描，不把已有18环境称测试集或泛化结果。若仍有边界失效，完成真实交付而不是无限调参。

## 五、评价口径

明确并列输出：
1. 几何/事件完成；2. 任务段约束有效；3. 完整窗口约束有效；4. 车身重叠/硬动作越界；5. 动态余量违例。

身体净距与h不同；各车各时刻的标记不是独立事故。jerk违例、紧急控制例外与普通饱和分开。

代价仍为全部四车固定16秒速度缺口距离，单位m。保留全部环境结果及准备/执行/交接/正常跟驰/失败处置分段。若任务阶段定义改变，给出旧新口径映射，不跨版本直接排名。

配对比较至少包括全部环境、双方任务段有效、双方完整窗口有效，报告环境ID和分母；不得只比较各方法自己的成功均值。共同成功子集只是条件化描述，不冒充总体因果估计。不删除失败代价，也不把不完成任务的低成本当优势。

记录完成状态 h、相对速度/加速度、后车制动需求、交接持续时间和恢复时间，使成本能够联系到机动末态，而不是只解释成“同步更好”。

动作链至少是名义→执行器限幅→安全投影→实际；如其中两层数值相同则说明，不伪造独立信号。直接使用公共层diag，避免actual!=nominal一律叫安全修正。

运行耗时记录每次决策候选数与预测步工作量、均值/高分位/最大值。不同方法进入完整网格次数不同，因此平均运行耗时不是同一工作量下的算法复杂度证明。

## 六、创新定位与交付

写一页实际模型—程序映射：X是什么、z究竟是什么、空间→z与z→空间通道在哪里、伙伴作用改变什么、谁在中央求解、哪些信息需要通信。保留清楚的swarmalator启发，不声称已复现原始周期振子或分布式群体行为。

如果公共预测/交接解决了主要问题，将其归于公共能力。若反馈改善代价但不增加任务数，按这个结果写。若非互易在部分角色压力条件下有利而非普遍有利，明确条件。不得把“必须S3胜出”作为验收目标。

交付：
- docs/model_v4.md，解释必要变化，附少量关键方程；
- reports/stage2b_report.md，先核心科学结果后必要实现说明；
- outputs/stage2b的主指标、有效性、成对差值、阶段代价、计划/交接/动作事件、关键轨迹、机制干预；
- 三组核心图：完整协作至恢复链条；相同服务条件下的成对结果；耦合方向与动作分担。不同单位不要混在一根纵轴；
- tests包含对照开关、停止边界、冻结候选全流程一致性、交接后动态条件、日志/错误字段的少量针对性测试；
- feedback/stage2b_feedback.zip：本轮所有必需导入文件、配置、模型报告、关键数据图表、复现入口。不要含虚拟环境、缓存、密钥、论文全文或无关旧实验。

请继续到真实运行、结果检查和反馈包生成，不在计划或未运行代码阶段结束。保留当前证据，完成本轮范围后停止扩张实验。返回真实数量、关键结果、剩余问题和复现命令。
