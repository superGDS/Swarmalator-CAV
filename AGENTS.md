# Stage 1 project notes

This project is a small, reproducible Swarmalator–CAV merge prototype. Preserve generated evidence and failed outcomes. Do not use phase synchronization as a substitute for finite-body motion or safety checks. Keep the four-vehicle scope and the five configured methods unless a later research instruction changes the stage boundary.

Common commands:

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m src.swarmalator_cav.run_stage1 --config configs/stage1_config.json --out outputs/stage1
```

Current report: `reports/stage1_report.md`.
